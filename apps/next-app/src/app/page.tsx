import { Button } from "@repo/ui/components/button";

export default function HomePage() {
  return (
    <main className="flex h-dvh animate-in items-center justify-center fade-in zoom-in">
      <Button>Click me</Button>
    </main>
  );
}
